<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 1. The Media Content -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 2. Top Bar: File Name (Permanently Hidden for Immersive View) -->
    <TextView
        android:id="@+id/txtName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="16dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:shadowColor="#80000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="3"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        android:visibility="gone"
        app:layout_constraintEnd_toStartOf="@id/imgFullscreen"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 3. Top Bar: Fullscreen Toggle -->
    <se.arctosoft.vault.views.PressableImageView
        android:id="@+id/imgFullscreen"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:padding="8dp"
        android:src="@drawable/ic_round_fullscreen_24"
        app:tint="@android:color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 4. Bottom Action Bar (Floating Glass Pill Design) -->
    <LinearLayout
        android:id="@+id/lLButtons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.card.MaterialCardView
            android:layout_width="190dp"
            android:layout_height="65dp"
            android:layout_marginBottom="7dp"
            app:cardBackgroundColor="#26FFFFFF"
            app:cardCornerRadius="32dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:paddingHorizontal="16dp"
                android:paddingVertical="6dp">

                <ImageButton
                    android:id="@+id/btnDelete"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:layout_marginHorizontal="4dp"
                    android:background="?selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_outline_delete_forever_24"
                    app:tint="#FFFFFF" />

                <ImageButton
                    android:id="@+id/btnExport"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:layout_marginHorizontal="4dp"
                    android:background="?selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_outline_file_download_24"
                    app:tint="#FFFFFF" />

                <ImageButton
                    android:id="@+id/btnMenu"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:layout_marginHorizontal="4dp"
                    android:background="?selectableItemBackgroundBorderless"
                    android:src="@drawable/round_more_vert_24"
                    app:tint="#FFFFFF" />

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>

    <!-- 5. UI Element Grouping -->
    <androidx.constraintlayout.widget.Group
        android:id="@+id/uiOverlayGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:constraint_referenced_ids="txtName,imgFullscreen,lLButtons" />

</androidx.constraintlayout.widget.ConstraintLayout>